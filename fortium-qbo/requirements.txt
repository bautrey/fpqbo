# Core
# Web stack pinned to exact versions: unpinned ranges let a fresh Render build
# pull a newer Starlette whose changed TemplateResponse signature 500'd the
# admin UI (June 2026). Pin fastapi + its starlette dep + uvicorn so redeploys
# are reproducible. Bump deliberately, not by surprise-on-rebuild.
fastapi==0.135.1
starlette==0.52.1
uvicorn[standard]==0.41.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0  # PostgreSQL driver

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Web
python-multipart>=0.0.6
jinja2>=3.1.0
itsdangerous>=2.1.0

# QuickBooks SDK
python-quickbooks>=0.9.0
intuit-oauth>=1.2.0

# HTTP clients
httpx>=0.26.0
authlib>=1.3.0
# requests is pulled in transitively by python-quickbooks; declared directly
# because qbo_service imports requests.exceptions to classify raw network
# (connect/read-timeout) errors for transient-retry.
requests>=2.31.0
